# 2026-03-21 工作日志

## 安全审计任务

对 `/Users/zzx/WorkBuddy/Claw` 项目进行全面安全扫描，发现并修复以下问题：

### 已修复
- `publish_news.py`：移除硬编码的微信公众号 AppID/AppSecret，改为 dotenv 读取
- `weapp/server/app.py`：移除硬编码的小程序 AppSecret 和公众号 AppSecret，改为环境变量；Flask 监听地址从 `0.0.0.0` 改为 `127.0.0.1`
- `wechat_config.example`：真实 AppID 替换为占位符，补充完整的配置模板

### 待用户手动处理（高危）
- 微信公众号 AppSecret 已泄露 → **必须立即在公众平台重置**
- 微信小程序 AppSecret 已泄露 → **必须立即重置**
- 阿里云 AccessKey `dinglo2iysmn6ij7n0g4` 已泄露 → **必须立即禁用并新建**

### 第二轮修复（08:32）
- `weapp/server/app.py`：新增 `require_auth` 装饰器，`/api/publish`、`/api/config`、`/api/news` 均加了 Token 鉴权；`/api/status` 保持公开
- `weapp/server/app.py`：日志改为只记录 errcode/errmsg，不再记录完整响应（修复 M-2）
- `hot_news_h5.html`：`publishNow()` 改为携带 `X-API-Token` Header，端口从 8080 修正为 5000，`/api/status` 响应字段也更新

### 密钥加密方案（09:12）
- 新增 `keyring_manager.py`：安全密钥管理工具，支持 migrate/list/show 子命令
- 密钥已迁移至 macOS 钥匙串（`SERVICE = "ClawWechatBot"`），包括微信公众号AppID/AppSecret和阿里云Key
- `publish_news.py` 和 `weapp/server/app.py` 均改为优先从钥匙串读取，`get_secret()` 兼容 fallback 到 .env
- `requirements.txt` 新增 `keyring>=25.0.0`
- `.env` 中的明文密钥仍保留（用户可手动删除）

### 尚未修复（记录在 security_audit.md）
- 缺少 CSP 头（M-3，低优先级）
- `.env` 中仍有明文密钥，建议重置后删除
- `API_TOKEN` 需补充到 `.env` 并填入 `hot_news_h5.html`

### 更新文件
- `security_audit.md` 已全面更新，含漏洞详情和修复建议
